"""The pitch-pilot pipeline graph.

In P0 this package holds only the typed state contract
(`PipelineState`). The deterministic outer graph
that wires the nodes together (`build_pipeline`)
is implemented in P1.
"""
