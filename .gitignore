# ---- pitch-pilot ----
# Local JsonStore output (the dev persistence + review queue).
# Root-anchored so intentional JSONL fixtures (e.g. evals/datasets/*.jsonl) stay tracked.
/pitch_pilot_store.jsonl
/pitch_pilot_store.review.jsonl

# MkDocs build output / Material cache
/site/
.cache/

# Secrets
.env
.env.*
!.env.example

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Validation harness raw output (regenerable; report lives in docs/validation/)
scripts/p1_validation_data.json

# Eval harness runtime artifacts (regenerable; reports are kept)
evals/cache/
evals/results/
